apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plane-app
  namespace: argocd
spec:
  project: default
  source:
    chart: plane-ce
    repoURL: https://helm.plane.so/
    targetRevision: 1.2.2
    helm:
      releaseName: my-plane
      valuesObject:
        planeVersion: stable
        ingress:
          appHost: "plane.nexus-core.dev"
          minioHost: "plane-minio.nexus-core.dev"
          ingressClass: "haproxy"
        redis:
          storageClass: "longhorn"
        postgres:
          storageClass: "longhorn"
        minio:
          storageClass: "longhorn"
        rabbitmq:
          storageClass: "longhorn"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: plane
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
      - ServerSideApply=false
