---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: placeholder-configdir-pvc
  namespace: placeholder
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
