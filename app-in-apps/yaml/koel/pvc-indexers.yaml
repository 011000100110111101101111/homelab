apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: koel-search_indexers-pv
  namespace: koel
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-static
  resources:
    requests:
      storage: 30Gb
