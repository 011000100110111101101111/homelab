apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: koel-media-pv
  namespace: koel
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-static
  resources:
    requests:
      storage: 1Ti
